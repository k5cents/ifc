% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq.R
\name{ifc_seq}
\alias{ifc_seq}
\title{Generate a sequence of IFC dates}
\usage{
ifc_seq(from, to = NULL, by, length.out = NULL)
}
\arguments{
\item{from}{An \code{ifc_date} of length 1. The start of the sequence.}

\item{to}{An \code{ifc_date} of length 1, or \code{NULL}. The inclusive end of the
sequence (reached exactly when evenly divisible; otherwise the sequence
stops before overshooting).}

\item{by}{A string: one of \code{"day"}, \code{"week"}, \code{"month"}, or \code{"year"}.}

\item{length.out}{A positive integer giving the desired sequence length, or
\code{NULL}.}
}
\value{
An \code{ifc_date} vector.
}
\description{
Creates a regular sequence of \code{ifc_date} values using IFC-native units.
}
\details{
\code{by = "day"} and \code{by = "week"} use fixed epoch offsets (1 and 7 days).
\code{by = "month"} uses \strong{calendar arithmetic}: the result preserves the same
IFC day-of-month, advancing one month at a time and wrapping across year
boundaries (month 13 wraps to month 1 of the next year). This correctly
skips Leap Day when crossing the June/Sol boundary in a leap year.
\code{by = "year"} similarly preserves IFC month and day, incrementing only the
year.

Exactly one of \code{to} or \code{length.out} must be supplied. Direction (ascending
or descending) is inferred from \code{from} and \code{to} automatically.

Year Day and Leap Day are included in sequences whenever the arithmetic
naturally lands on them (e.g. stepping by \code{"day"} across year-end will
pass through Year Day).
}
\examples{
# First day of all 13 months in 2024
ifc_seq(ifc_ymd(2024, 1, 1), by = "month", length.out = 13)

# Weekly sequence between two dates
ifc_seq(ifc_ymd(2024, 1, 1), ifc_ymd(2024, 4, 1), by = "week")

# Jan 1 for six consecutive years
ifc_seq(ifc_ymd(2020, 1, 1), ifc_ymd(2025, 1, 1), by = "year")
}
