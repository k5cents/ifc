% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/round.R
\name{ifc_round}
\alias{ifc_round}
\alias{ifc_floor}
\alias{ifc_ceiling}
\title{Round an IFC date to a unit boundary}
\usage{
ifc_floor(x, unit)

ifc_ceiling(x, unit)

ifc_round(x, unit)
}
\arguments{
\item{x}{An \code{ifc_date} vector.}

\item{unit}{A string: \code{"week"}, \code{"month"}, or \code{"year"}.}
}
\value{
An \code{ifc_date} vector the same length as \code{x}.
}
\description{
Round, floor, or ceiling an \code{ifc_date} to the nearest \code{"week"}, \code{"month"},
or \code{"year"} boundary. IFC's fixed-length units make these operations exact:
every week is 7 days, every month 28 days.
}
\details{
Boundary definitions:
\itemize{
\item \strong{week}: the Sunday (day 1 of a week) that starts the 7-day period.
\item \strong{month}: day 1 of the IFC month.
\item \strong{year}: January 1 of the IFC year.
}

Intercalary day behaviour:
\itemize{
\item Year Day and Leap Day always follow a Saturday (day 28 of a month), so
their week floor is 7 days back (the preceding Sunday) and their month
floor is 28 days back (day 1 of the preceding month).
\item Ceiling always advances to the next regular boundary: Leap Day ceiling =
Sol 1; Year Day ceiling = January 1 of the following year.
}

\code{ifc_round()} picks whichever boundary is closer. Ties (e.g. day 15 for
\code{"month"}) round toward the floor.
}
\examples{
x <- ifc_ymd(2024, 7, 14)
ifc_floor(x, "week")    # Sol 8  (Sunday of the same week)
ifc_floor(x, "month")   # Sol 1
ifc_floor(x, "year")    # Jan 1, 2024
ifc_ceiling(x, "week")  # Sol 15 (next Sunday)
ifc_ceiling(x, "month") # Aug 1  (day 1 of next month)
ifc_ceiling(x, "year")  # Jan 1, 2025
ifc_round(x, "week")    # Sol 15 (day 14 = Saturday, 1 day from Sol 15)
ifc_round(x, "month")   # Sol 1  (day 14 is in the first half; ties go to floor)
ifc_round(x, "year")    # Jan 1, 2024
}
