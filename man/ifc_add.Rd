% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add.R
\name{ifc_add}
\alias{ifc_add}
\alias{add_weeks}
\alias{add_months}
\alias{add_years}
\title{Add IFC calendar units to a date}
\usage{
add_weeks(x, n)

add_months(x, n)

add_years(x, n)
}
\arguments{
\item{x}{An \code{ifc_date} vector.}

\item{n}{Integer number of units to add (may be negative).}
}
\value{
An \code{ifc_date} vector.
}
\description{
Add a whole number of weeks, months, or years to an \code{ifc_date} vector using
IFC calendar arithmetic. Both \code{x} and \code{n} are recycled to a common length.
}
\details{
\itemize{
\item \code{add_weeks(x, n)}: adds \code{n * 7L} days (always exact).
\item \code{add_months(x, n)}: calendar arithmetic — preserves the IFC day-of-month
and advances the month by \code{n}, wrapping across year boundaries. This
correctly handles the June/Sol boundary in leap years.
\item \code{add_years(x, n)}: calendar arithmetic — preserves the IFC month and
day, advancing the year by \code{n}. For Leap Day inputs, the result is the
Leap Day of the target year; a non-leap target year produces an error.
}

Negative \code{n} moves backwards.
}
\examples{
add_weeks(ifc_ymd(2024, 1, 1), 4)    # Feb 1, 2024 (+28 days)
add_months(ifc_ymd(2024, 1, 1), 6)   # Sol 1, 2024 (skips Leap Day)
add_months(ifc_ymd(2024, 13, 1), 1)  # Jan 1, 2025 (wraps to next year)
add_years(ifc_ymd(2024, 7, 1), 1)    # Sol 1, 2025
add_years(ifc_ymd(2024, 7, 1), -1)   # Sol 1, 2023
}
